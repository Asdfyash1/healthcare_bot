# ML Models Directory
# Place the following files here when ready:
# - chatbot_model.h5    (Keras/TensorFlow CNN model)
# - tokenizer.pkl       (Fitted Keras tokenizer)
# - label_encoder.pkl   (Fitted LabelEncoder)
#
# After placing the files, update chatbot/views.py to load and use them.
